# Pending labs

Labs that were read, analysed or started and then set aside. **Remove an entry from this list when its file lands in `labs/`.**

Anything not on this list either has a file in `labs/` or has never been looked at.

## The list

**Empty as of 2026-08-19.** Every lab that has been read or started now has a file in `labs/`.

When an entry is added back, put it in a table with the lab id, the name and its state, then a section below with what is already known about it: the split between shell and console, the values that have to be read off the started page, the order that matters, and anything that failed before. The point of an entry is that a future attempt starts ahead rather than level.

## Not pending

- **GSP364**, Managed Service for Prometheus: Challenge Lab, already has a file at `labs/gsp364-managed-prometheus-challenge.md` and will not be repeated. Check the README index before starting anything that feels familiar; it caught that one before a lab attempt was spent.
