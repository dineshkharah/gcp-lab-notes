# Labs done before these notes started

Short entries only. These were completed before the repo existed, so the commands were not kept. Listed so the record is complete.

- GSP367, Document AI pipeline. Note kept at the time: there is no `gcloud documentai`, so the api has to be called over rest.
- ADK challenge lab, Cymbal Travel agents.
- CI/CD lab using Cloud Deploy.
- A gcloud iam and service accounts lab.
- arc102, the thumbnail Cloud Function lab. This is where the gen2 requirement was first found: a gen1 function deploys and works and the scorer still rejects it.
